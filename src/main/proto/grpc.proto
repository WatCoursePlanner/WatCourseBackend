syntax = "proto2";

package com.watcourses.wat_courses.proto;

import "courses.proto";

message CourseInfoRequest {
  required string code = 1;
}

service CourseService {
  rpc getCourseInfo (CourseInfoRequest) returns (CourseInfo);
}
