// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: courses.proto
package com.watcourses.wat_courses.proto

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.WireField
import com.squareup.wire.internal.redactElements
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Nothing
import kotlin.String
import kotlin.collections.List
import kotlin.hashCode
import kotlin.jvm.JvmField
import okio.ByteString

class SearchCourseResponse(
  @field:WireField(
    tag = 1,
    adapter = "com.watcourses.wat_courses.proto.PaginationInfoResponse#ADAPTER"
  )
  val pagination: PaginationInfoResponse? = null,
  @field:WireField(
    tag = 2,
    adapter = "com.watcourses.wat_courses.proto.CourseInfo#ADAPTER",
    label = WireField.Label.REPEATED
  )
  val results: List<CourseInfo> = emptyList(),
  unknownFields: ByteString = ByteString.EMPTY
) : Message<SearchCourseResponse, Nothing>(ADAPTER, unknownFields) {
  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN
  )
  override fun newBuilder(): Nothing = throw AssertionError()

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is SearchCourseResponse) return false
    return unknownFields == other.unknownFields
        && pagination == other.pagination
        && results == other.results
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + pagination.hashCode()
      result = result * 37 + results.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (pagination != null) result += """pagination=$pagination"""
    if (results.isNotEmpty()) result += """results=$results"""
    return result.joinToString(prefix = "SearchCourseResponse{", separator = ", ", postfix = "}")
  }

  fun copy(
    pagination: PaginationInfoResponse? = this.pagination,
    results: List<CourseInfo> = this.results,
    unknownFields: ByteString = this.unknownFields
  ): SearchCourseResponse = SearchCourseResponse(pagination, results, unknownFields)

  companion object {
    @JvmField
    val ADAPTER: ProtoAdapter<SearchCourseResponse> = object : ProtoAdapter<SearchCourseResponse>(
      FieldEncoding.LENGTH_DELIMITED, 
      SearchCourseResponse::class, 
      "type.googleapis.com/com.watcourses.wat_courses.proto.SearchCourseResponse"
    ) {
      override fun encodedSize(value: SearchCourseResponse): Int = 
        PaginationInfoResponse.ADAPTER.encodedSizeWithTag(1, value.pagination) +
        CourseInfo.ADAPTER.asRepeated().encodedSizeWithTag(2, value.results) +
        value.unknownFields.size

      override fun encode(writer: ProtoWriter, value: SearchCourseResponse) {
        PaginationInfoResponse.ADAPTER.encodeWithTag(writer, 1, value.pagination)
        CourseInfo.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.results)
        writer.writeBytes(value.unknownFields)
      }

      override fun decode(reader: ProtoReader): SearchCourseResponse {
        var pagination: PaginationInfoResponse? = null
        val results = mutableListOf<CourseInfo>()
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> pagination = PaginationInfoResponse.ADAPTER.decode(reader)
            2 -> results.add(CourseInfo.ADAPTER.decode(reader))
            else -> reader.readUnknownField(tag)
          }
        }
        return SearchCourseResponse(
          pagination = pagination,
          results = results,
          unknownFields = unknownFields
        )
      }

      override fun redact(value: SearchCourseResponse): SearchCourseResponse = value.copy(
        pagination = value.pagination?.let(PaginationInfoResponse.ADAPTER::redact),
        results = value.results.redactElements(CourseInfo.ADAPTER),
        unknownFields = ByteString.EMPTY
      )
    }
  }
}
